apiVersion: v1
kind: Service
metadata:
    name: moraghebreactandspringdatarest
spec:
    type: LoadBalancer
    ports:
    - port: 8080 
    selector:
        app: moraghebreactandspringdatarest